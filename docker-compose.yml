version: "1.0"
services:
  worker:
    build: .
    environment:
      API_ID: $API_ID
      API_HASH: $API_HASH
      ALIVE_NAME: $ALIVE_NAME
      BOT_TOKEN: $BOT_TOKEN
      SESSION_NAME: $SESSION_NAME
      SUDO_USERS: $SUDO_USERS
      BOTLOG_CHATID: $BOTLOG_CHATID
